.page-header
  %h1=@genre.name

.list-group
  -@books.each do |book|
    =render 'books/book', book: book

-if Genre.manage?(@current_user)
  .btn-toolbar
    =link_to edit_genre_path(@genre),class: 'btn btn-primary' do
      =fa_icon 'pencil'
      Редактировать
%p

=paginate @books